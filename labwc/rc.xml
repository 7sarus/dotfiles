<?xml version="1.0"?>
<!-- This file contains all supported config elements and attributes -->
<labwc_config>

  <core>
    <decoration>server</decoration>
    <gap>10</gap>
  </core>

  <theme>
    <name>Raven-Cyan</name>
    <cornerRadius>6</cornerRadius>

    <!-- Font can be defined without attributues to set all places the same -->
    <font place="ActiveWindow"><name>Barlow Condensed</name><size>12</size></font>
    <font place="MenuItem"><name>Barlow Condensed</name><size>14</size></font>
  </theme>

  <focus>
    <followMouse>no</followMouse>
    <raiseOnFocus>no</raiseOnFocus>
  </focus>

  <!--
    Keybind actions are specified in more detail in labwc-actions(5)
    The following keybind modifiers are supported:
      W - window/super/logo
      A - alt
      C - ctrl
      S - shift
  -->

  <keyboard>

    <!--
      The element <default /> to load the default keybind and thus avoid
      repeating all the definitions below.
    -->
    <!-- Window Actions-->
    <keybind key="A-Escape">
      <action name="Exit"/>
    </keybind>
    <keybind key="A-Tab">
      <action name="NextWindow"/>
    </keybind>
    <keybind key="A-F4">
      <action name="Close"/>
    </keybind>
    <keybind key="W-a">
      <action name="ToggleMaximize"/>
....</keybind>
    <keybind key="W-f">
      <action name="ToggleFullscreen"/>
    </keybind>
    <keybind key="A-Left">
      <action name="MoveToEdge"><direction>left</direction></action>
    </keybind>
    <keybind key="A-Right">
      <action name="MoveToEdge"><direction>right</direction></action>
    </keybind>
    <keybind key="A-Up">
      <action name="MoveToEdge"><direction>up</direction></action>
    </keybind>
    <keybind key="A-Down">
      <action name="MoveToEdge"><direction>down</direction></action>
    </keybind>
    <!-- Application Shortcuts-->
    <keybind key="W-Return">
      <action name="Execute"><command>kitty</command></action>
    </keybind>
    <keybind key="A-F3">
      <action name="Execute"><command>wofi --show run</command></action>
    </keybind>
  </keyboard>

  <mouse>
    <!-- time is in ms -->
    <doubleClickTime>500</doubleClickTime>
    <context name="TitleBar">
      <mousebind button="Left" action="DoubleClick">
        <action name="ToggleMaximize"/>
      </mousebind>
    </context>
  </mouse>

</labwc_config>
